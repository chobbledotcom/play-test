<%
  # Pass radio button partial
  # Required parameters:
  #   field: Field name (e.g., 'has_slide' or 'slide_platform_height_pass')
  #   prefilled: Whether the field has been prefilled
  #   checked_value: The currently checked value (true, false, or nil)
  #   i18n_key: The i18n key for the label (e.g., 'shared.pass' or 'shared.yes')
  # Optional parameters:
  #   wrapper_class: Additional wrapper class (default: 'pass')
  
  form = @_current_form
  wrapper_class = local_assigns.fetch(:wrapper_class, 'pass')
%>

<%= render 'form/field_wrapper',
  prefilled: prefilled,
  wrapper_class: wrapper_class do
%>
  <label>
    <%= t(i18n_key) %>
    <%= form.radio_button field,
      true,
      radio_button_options(prefilled, checked_value, true)
    %>
  </label>
<% end %>