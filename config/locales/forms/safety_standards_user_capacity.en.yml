en:
  forms:
    safety_standards_user_capacity:
      header: Calculate User Capacity
      sections:
        dimensions: Playing Area Dimensions
        user_height: User Height Limit
        result: Calculation Results
      fields:
        length: Length (m)
        width: Width (m)
        negative_adjustment_area: Obstacle/Adjustment Area (m²)
        max_user_height: Maximum User Height (m)
      submit: Calculate Capacity