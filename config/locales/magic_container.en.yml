# Operator-facing strings for the Magic Container wizard and the libraries
# it drives. The wizard runs in a console, not a browser.
en:
  magic_container:
    bunny_client:
      errors:
        api_error: "Bunny API error: %{label}"
    litestream_seeder:
      errors:
        command_failed: "Litestream command failed: %{command}\n%{detail}"
        no_snapshot: "No Litestream snapshot appeared in %{bucket} for %{replica_path}. Last listing:\n%{listing}"
    prompts:
      hidden_answer: "(hidden input)"
      input_closed: "Input closed before the question was answered"
      not_a_valid_choice: "Not a valid choice."
      number: "Number"
      required: "A value is required."
    wizard:
      banners:
        deployed: "Deployed"
        main: "Bunny Magic Container from a backup"
        plan: "Plan"
      errors:
        archive_listing_failed: "Could not list %{archive}"
        archive_not_found: "Archive not found: %{path}"
        backup_date_missing: "Cannot read a backup date from %{name}"
        endpoint_missing: "No container endpoint appeared for app %{id}"
        no_databases: "No databases found in %{archive}"
      notes:
        app_reused: "Reusing app %{id} created by a previous attempt"
        base_url_set: "BASE_URL set to %{url}"
        creating_app: "Creating the Magic Container app..."
        env_saved: "Environment record saved to %{path}"
        files_skipped:
          one: "Skipped %{count} Active Storage file already on S3"
          other: "Skipped %{count} Active Storage files already on S3"
        loaded_answers: "Loaded answers from %{path}"
        master_key_invalid: "RAILS_MASTER_KEY must be 32 hexadecimal characters"
        master_key_replenishing: "The stored RAILS_MASTER_KEY is malformed - re-entering"
        orphan_reused: "Reusing existing Bunny app %{id}"
        report_app: "App: %{app} (id %{id})"
        report_base_url: "BASE_URL is set to %{url}"
        report_first_boot: "First boot restores databases via Litestream"
        report_url: "URL: %{url}"
        report_workdir: "Working files kept in %{path}"
        restored_databases: "Restored databases: %{names}"
        restoring: "Restoring backup and uploading Active Storage files..."
        seeded: "Seeded %{name} to %{replica_path}"
        seeded_skip: "Litestream replica for %{name} already seeded by a previous attempt - skipping"
        seeding: "Seeding Litestream replicas from the restored databases..."
        volume_size_invalid: "Enter a positive whole number of gigabytes"
        waiting_endpoint: "Waiting for the container endpoint..."
      plan:
        app: "App: %{app} (%{runtime}) in %{region}"
        archive: "Archive: %{archive}"
        db_restore: "Databases will be restored from the archive and seeded to Litestream"
        image: "Image: %{image}"
        litestream: "Litestream S3: %{bucket} at %{endpoint}"
        secret_generated: "A new SECRET_KEY_BASE was generated"
        storage: "Active Storage S3: %{bucket} at %{endpoint}"
        storage_upload: "Active Storage files will be uploaded to the S3 bucket"
        volume_with: "%{size}GB persistent volume"
        volume_without: "none, databases re-restore from Litestream on boot"
      questions:
        app_name: "Bunny application name"
        archive_path: "Backup archive path"
        attach_volume: "Attach a persistent volume for the databases?"
        base_url: "Public base URL (blank to use container URL)"
        bunny_access_key: "Bunny API access key"
        bunny_key_from_env: "Use the BUNNY_ACCESS_KEY from your environment?"
        confirm_plan: "Create the Magic Container with these settings?"
        confirm_reuse: "Reuse the previous attempt's answers from %{path}?"
        display_app_name: "Display app name (APP_NAME)"
        image: "Image (namespace/name)"
        image_tag: "Image tag"
        litestream_banner: "Litestream S3 (database replicas)"
        master_key: "RAILS_MASTER_KEY"
        optimal_region: "Use the optimal region (%{region})?"
        orphan_choice: "%{name} (id %{id})"
        orphan_create: "Create a new app instead"
        orphan_select: "Several Bunny apps are named %{name} - which should this deployment use?"
        reuse_orphan: "Found a Bunny app named %{name} (id %{id}) - reuse it for this deployment?"
        region: "Region"
        registry: "Image registry"
        runtime_reserved: "Reserved (dedicated resources)"
        runtime_shared: "Shared (cheaper, burstable resources)"
        runtime_type: "Runtime type"
        s3_access_key_id: "Access key id"
        s3_bucket: "Bucket"
        s3_endpoint: "Endpoint (e.g. https://storage.bunnycdn.com)"
        s3_region: "Region"
        s3_secret_key: "Secret access key"
        sentry_dsn: "Sentry DSN"
        storage_banner: "Active Storage S3 (uploaded files)"
        volume_size: "Volume size (GB)"
